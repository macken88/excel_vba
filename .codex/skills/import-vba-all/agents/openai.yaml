interface:
  display_name: "Import VBA All"
  short_description: "Import tracked VBA into workbooks"
  default_prompt: "Use $import-vba-all to import tracked VBA source into all configured workbooks."
